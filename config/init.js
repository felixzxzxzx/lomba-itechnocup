const appConfig = require('./app.conf');
const dbConfig = require('./db.conf');
const appKeys = require('./app.keys');

module.exports = {
    // app: appConfig,
    db: dbConfig,
    // keys: appKeys
};