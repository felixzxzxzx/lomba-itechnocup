{
  "name": "foodbridge",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix front-end run dev",
    "lint": "npm --prefix front-end run lint",
    "build": "npm --prefix front-end run build",
    "dev:backend": "npm --prefix back-end run dev",
    "db:fresh": "npm --prefix back-end run db:fresh"
  }
}